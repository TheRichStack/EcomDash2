# Connector Common Runtime

This folder is for reusable connector infrastructure shared across standalone jobs.

Current contents:

- connector interface/contracts
- enabled and strict gating
- retry and timeout helpers
- stub-connector creation for the first runtime backbone

Connector-specific transforms should stay in their own connector folders.
